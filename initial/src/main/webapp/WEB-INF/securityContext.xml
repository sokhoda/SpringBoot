<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation=
                 "http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
				  http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <http pattern="/app/**">
        <intercept-url pattern="/app/loginp*" access="isAnonymous()" method="GET"/>
        <intercept-url pattern="/**" access="isAuthenticated()"/> <!--isAuthenticated-->
        <form-login
                login-page="/app/loginp"
                always-use-default-target="true"
                default-target-url="/app/dashboard"
                login-processing-url="/app/login"
                authentication-failure-url="/app/errorPage"/>
        <logout logout-url="/app/logout"/>
        <csrf disabled="true"/>
    </http>

    <http pattern="/rest/**" security="none"/>
    <!--<http pattern="/rest/**" create-session="stateless">-->
    <!--<http-basic entry-point-ref="authEntryPoint"/>-->
    <!--<intercept-url pattern="/**" access="hasRole('USER')"/>-->
    <!--<csrf disabled="false"/>-->
    <!--</http>-->

    <!--<http/>-->
    <!--<user-service>-->
    <!--<user name="user" password="password" authorities="ROLE_USER"/>-->
    <!--</user-service>-->

    <authentication-manager>
        <ldap-authentication-provider
                user-search-base="ou=people"
                user-search-filter="(uid={0})"
                group-search-base="ou=groups"
                group-search-filter="(member={0})">
        </ldap-authentication-provider>
    </authentication-manager>

    <ldap-server root="dc=regional,dc=com" ldif="users.ldif"/>

</b:beans>